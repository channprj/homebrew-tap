# homebrew-tap

Homebrew tap. Written by chann.

## Install

```bash
brew install channprj/tap/kmsg
brew install channprj/tap/pdf-to-typst
```

`pdf-to-typst` is currently installed from source on macOS and vendors its helper
scripts into Homebrew's `lib/pdf-to-typst/tools` path.
